# This project uses the public.com CLI for authentication.
# No credentials are stored by this application.
#
# Before running, install and authenticate the CLI:
#   pipx install publicdotcom-cli
#   public auth login
#
# See: https://public.com/api/docs/templates/publicdotcom-cli
